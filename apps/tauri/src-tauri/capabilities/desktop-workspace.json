{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-workspace",
  "description": "Allows the main window to import, export, and autosave workspace packages.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "log:default"
  ]
}
