{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-images",
  "description": "Allows the main window to read image files and list image folders.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "log:default"
  ]
}
